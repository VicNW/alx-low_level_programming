#include <stdio.h>

/**
* main - This code prints the alphabets in lowercase
* Return: always 0, successful.
*/
int main(void)
{
	int x = 'a';
	while (x <= 'z')
	{
	putchar(x);
	x++;
	return (0);
	}
}
