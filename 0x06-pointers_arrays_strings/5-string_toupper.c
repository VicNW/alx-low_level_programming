#include "main.h"

/**
* string_toupper - changes strings to uppercase
* @str: input string
* Return: Uppercase of str
*/

char *string_toupper(char *)
{
	int i;

	if (str[i] >= 65 && <= 90)
	{
		str[i];
	}
	i++
	else
	{
		str[i] -= 32;
	}
	return str;
}
