#include "main.h"

/**
* cap_string - capitalizes the first letter of each word
* @str: input string
* Return: str
*/

char *cap_string(char *)
{
	int i = 0;
	
	if (str[0] >= 97 && <= 122)
	{
		str[0] -= 32;
	}
	else
	{
		str[0];
	}
	if (str[i] = ' ')
	{
	i++
	while (str[i] >= 97 && <= 122)
	{
		str[i] -= 32;
	}
	else 
	{
		str[i]
	}
	i++ 
	}
	return str;
}
